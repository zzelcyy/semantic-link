import { AppLayout } from '@/components/layout/AppLayout';

function App() {
  return (
    <AppLayout>
      <section className="mx-auto max-w-4xl space-y-6">
        Import CSV files and create relationships between datasets. [ Import CSV ]
      </section>
    </AppLayout>
  );
}

export default App;
